# Database (SQLite for Replit)
DATABASE_URL="file:./prisma/dev.db"

# Server
PORT=3001
NODE_ENV=production

# Session
SESSION_SECRET=tea-inventory-replit-secret-key-2024

# CORS (will be auto-detected from Replit environment)
# CORS_ORIGIN will be set automatically based on REPL_SLUG and REPL_OWNER

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100