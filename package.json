{
  "name": "tea-inventory-system",
  "version": "1.0.0",
  "description": "Tea Inventory Management System - Production ready with role-based access",
  "scripts": {
    "start": "bash deploy.sh",
    "dev": "cd backend && npm run dev",
    "build": "cd frontend && npm run build",
    "install-all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["inventory", "tea", "management", "production", "react", "nodejs"],
  "author": "Sushiramenai",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
