# Railway Environment Variables Template
# These variables should be set in Railway dashboard

# Backend Service Variables
NODE_ENV=production
PORT=3001

# Database (Railway will auto-inject DATABASE_URL when you add PostgreSQL)
# DATABASE_URL will be automatically provided by Railway PostgreSQL

# Security
SESSION_SECRET=your-very-secure-random-string-at-least-32-characters

# CORS - Update this to your Railway frontend URL
CORS_ORIGIN=https://your-frontend.up.railway.app

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Frontend Service Variables (set in frontend service)
REACT_APP_API_URL=https://your-backend.up.railway.app/api
REACT_APP_NAME=Tea Inventory System
REACT_APP_ENV=production